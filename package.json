{
  "name": "medihub",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace web",
    "dev:agent": "npm run dev --workspace agent",
    "build:web": "npm run build --workspace web",
    "build:agent": "npm run build --workspace agent",
    "agent:try": "npm run try --workspace agent"
  },
  "engines": {
    "node": ">=20"
  }
}
